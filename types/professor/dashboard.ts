export interface TestData {
  examId: number;
  examTitle: string;
  examStartDate: string;
  examEndDate: string;
  examStatus: string;
}

export interface LanguageGraph {
  lang: string;
  count: number;
}
